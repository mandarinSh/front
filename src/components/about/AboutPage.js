import React from "react";

const AboutPage = () => (
    <div className="page">
        <h2>{"Open Analyzer Features"}</h2>
        <br/>
        <h5>{"Using this app you can load logs from various courses. Loaded logs can be analyzed and the result can be drawn and loaded to your local computer."}</h5>
    </div>
);

export default AboutPage;

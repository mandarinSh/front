import React from "react";

const AboutPage = () => (
    <div className="page">
        <h2>{"About"}</h2>
        <p>{"This app uses React, Redux, React Router, and many other helpful libraries. (README will be available later)"}</p>
    </div>
);

export default AboutPage;
